describe('authHttpInterceptorService', function() {

  beforeEach(module('authentication'));

  it('should ...', inject(function(authHttpInterceptorService) {

	//expect(authHttpInterceptorService.doSomething()).toEqual('something');

  }));

});