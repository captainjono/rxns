describe('tokenManagerService', function() {

  beforeEach(module('authentication'));

  it('should ...', inject(function(tokenManagerService) {

	//expect(tokenManagerService.doSomething()).toEqual('something');

  }));

});