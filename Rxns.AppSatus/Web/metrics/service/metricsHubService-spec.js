describe('metricsHubService', function() {

  beforeEach(module('metrics'));

  it('should ...', inject(function(metricsHubService) {

	//expect(metricsHubService.doSomething()).toEqual('something');

  }));

});