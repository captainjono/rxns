describe('metricsService', function() {

  beforeEach(module('metrics'));

  it('should ...', inject(function(metricsService) {

	//expect(metricsService.doSomething()).toEqual('something');

  }));

});