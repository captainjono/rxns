describe('eventHubService', function() {

  beforeEach(module('portal'));

  it('should ...', inject(function(eventHubService) {

	//expect(eventHubService.doSomething()).toEqual('something');

  }));

});