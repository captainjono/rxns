describe('statusService', function() {

  beforeEach(module('systemstatus'));

  it('should ...', inject(function(statusService) {

	//expect(statusService.doSomething()).toEqual('something');

  }));

});